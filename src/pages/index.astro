---
// Fecha del evento (18 de octubre de 2025 a las 21:30)
const eventDate = new Date('2025-10-18T21:30:00');
---

<html lang="es">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Milena - 15 Años ✨</title>
		<link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;600;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
	</head>
	<body>
		<!-- Hero Section -->
	<section class="hero">
		<div class="hero-overlay"></div>
		<div class="hero-decorative-corners">
			<!-- Flores en las esquinas con rotaciones en sentido horario -->
			<div class="corner-decoration top-left">
				<img src="/flor-margen.png" alt="Decoración floral" class="floral-corner-image" />
			</div>
			<div class="corner-decoration top-right">
				<img src="/flor-margen.png" alt="Decoración floral" class="floral-corner-image rotated-90" />
			</div>
			<div class="corner-decoration bottom-right">
				<img src="/flor-margen.png" alt="Decoración floral" class="floral-corner-image rotated-180" />
			</div>
			<div class="corner-decoration bottom-left">
				<img src="/flor-margen.png" alt="Decoración floral" class="floral-corner-image rotated-270" />
			</div>
			
			<!-- Flores en el medio de cada borde -->
			<div class="middle-decoration center-top">
				<img src="/flor-medio-transparent.svg" alt="Decoración floral central" class="floral-middle-image" />
			</div>
			<div class="middle-decoration center-right">
				<img src="/flor-medio-transparent.svg" alt="Decoración floral central" class="floral-middle-image" />
			</div>
			<div class="middle-decoration center-bottom">
				<img src="/flor-medio-transparent.svg" alt="Decoración floral central" class="floral-middle-image" />
			</div>
			<div class="middle-decoration center-left">
				<img src="/flor-medio-transparent.svg" alt="Decoración floral central" class="floral-middle-image" />
			</div>
		</div>
		<div class="hero-content">
			<div class="decorative-frame">
				<div class="corner-ornament top-left"></div>
				<div class="corner-ornament top-right"></div>
				<div class="corner-ornament bottom-left"></div>
				<div class="corner-ornament bottom-right"></div>
				<div class="side-decoration left-side">✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦</div>
				<div class="side-decoration right-side">✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦</div>
				<h1 class="hero-title">Milena</h1>
				<p class="hero-subtitle">¡Celebra mis 15 años!</p>
			</div>
		</div>
	</section>		<!-- Countdown Section -->
		<section class="countdown-section">
			<div class="container">
				<h2 class="section-title">¡La fiesta está cerca!</h2>
				<div class="countdown" id="countdown">
					<div class="countdown-item">
						<span class="countdown-number" id="days">00</span>
						<span class="countdown-label">Días</span>
					</div>
					<div class="countdown-item">
						<span class="countdown-number" id="hours">00</span>
						<span class="countdown-label">Horas</span>
					</div>
					<div class="countdown-item">
						<span class="countdown-number" id="minutes">00</span>
						<span class="countdown-label">Minutos</span>
					</div>
					<div class="countdown-item">
						<span class="countdown-number" id="seconds">00</span>
						<span class="countdown-label">Segundos</span>
					</div>
				</div>
			</div>
		</section>

		<!-- Event Details Section -->
		<section class="event-details">
			<div class="container">
				<h2 class="section-title">Detalles del Evento</h2>
				<div class="event-info">
					<div class="event-item">
						<i class="fas fa-calendar-alt"></i>
						<div>
							<h3>Fecha</h3>
							<p>18 de Octubre, 2025</p>
						</div>
					</div>
					<div class="event-item">
						<i class="fas fa-clock"></i>
						<div>
							<h3>Hora</h3>
							<p>21:30 hs</p>
						</div>
					</div>
				</div>
				
				<div class="action-buttons">
					<a href="https://forms.gle/XSBFwzXy42Fjv5SN6" target="_blank" class="btn btn-primary">
						<i class="fas fa-check-circle"></i>
						Confirmar Asistencia
					</a>
					<button class="btn btn-secondary" onclick="addToCalendar()">
						<i class="fas fa-calendar-plus"></i>
						Agregar al Calendario
					</button>
				</div>
			</div>
		</section>

		<!-- Dress Code Section -->
		<section class="dress-code">
			<div class="container">
				<h2 class="section-title">Dress Code</h2>
				<div class="dress-code-content">
					<div class="dress-code-item">
						<i class="fas fa-tshirt"></i>
						<h3>Elegante Sport</h3>
						<p>Vení vestido para acompañar a la quinceañera</p>
					</div>
					<div class="color-palette">
						<h4>Colores sugeridos:</h4>
						<div class="colors">
							<div class="color-item" style="background: #D4BFAA; color: #333;">Nude</div>
							<div class="color-item" style="background: #8B7E74; color: white;">Marrón Taupe</div>
							<div class="color-item" style="background: #9CAF88; color: white;">Verde Salvia</div>
							<div class="color-item" style="background: #3C3C3C; color: white;">Gris Oscuro</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Location Section -->
		<section class="location">
			<div class="container">
				<h2 class="section-title">¿Dónde será?</h2>
				<div class="location-content">
					<div class="location-info">
						<h3>Janos Champagnat</h3>
						<p><i class="fas fa-map-marker-alt"></i> San Martín 71, Pilar, Provincia de Buenos Aires</p>
					</div>
					<div class="map-container">
						<iframe 
							src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3290.4472868468944!2d-58.9165049!3d-34.4519258!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x95bc9dfbf9512b21%3A0xf88b96fff239a654!2sJanos%20Champagnat!5e0!3m2!1ses!2sar!4v1635789012345!5m2!1ses!2sar" 
							width="100%" 
							height="300" 
							style="border:0;" 
							allowfullscreen="" 
							loading="lazy">
						</iframe>
					</div>
				</div>
			</div>
		</section>

		<!-- Music Suggestions Section -->
		<section class="music-suggestions">
			<div class="container">
				<h2 class="section-title">¡Ayúdanos con la Música!</h2>
				<div class="music-content">
					<p>¿Tienes alguna canción especial que te gustaría escuchar en la fiesta?</p>
					<p>¡Comparte tus sugerencias musicales para que la pista de baile esté súper animada!</p>
					<a href="https://forms.gle/TvhbW3bNQS9y9gZc7" target="_blank" class="btn btn-primary">
						<i class="fas fa-music"></i>
						Sugerir Música
					</a>
				</div>
			</div>
		</section>

		<!-- Gifts Section -->
		<section class="gifts">
			<div class="container">
				<h2 class="section-title">Regalos</h2>
				<div class="gifts-content">
					<p>Tu presencia es el mejor regalo, pero si querés regalar algo aún mejor...</p>
					<a href="/regalos" class="btn btn-primary">
						<i class="fas fa-gift"></i>
						Ver Opciones
					</a>
				</div>
			</div>
		</section>

		<!-- Footer -->
		<footer class="footer">
			<div class="container">
				<p>Con amor, Milena 💕</p>
				<p>¡Gracias por ser parte de mi día especial!</p>
				
				<div class="footer-contact">
					<p>¿Te gustó esta invitación? <a href="https://wa.me/541121619240" target="_blank" rel="noopener noreferrer" class="whatsapp-link"><i class="fab fa-whatsapp"></i> Contactame</a></p>
				</div>
				
				<div class="footer-credits">
					<p>Hecho por <a href="https://www.linkedin.com/in/kevin-caria/" target="_blank" rel="noopener noreferrer"><strong>kevincaria</strong></a></p>
				</div>
			</div>
		</footer>

		<style>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			body {
				font-family: 'Poppins', sans-serif;
				line-height: 1.6;
				color: #333;
			}

			.container {
				max-width: 1200px;
				margin: 0 auto;
				padding: 0 20px;
			}

		/* Hero Section */
		.hero {
			height: 100vh;
			background: linear-gradient(135deg, #ffbf1c 0%, #f8b877 100%);
			position: relative;
			display: flex;
			align-items: center;
			justify-content: center;
			text-align: center;
			color: white;
			overflow: hidden;
		}

		.hero-overlay {
			position: absolute;
			top: 0;
			left: 0;
			right: 0;
			bottom: 0;
			background: rgba(0,0,0,0.12);
		}

		/* Decoraciones florales de esquina */
		.hero-decorative-corners {
			position: absolute;
			top: 0;
			left: 0;
			right: 0;
			bottom: 0;
			pointer-events: none;
			z-index: 2;
		}

		.corner-decoration {
			position: absolute;
		}

		.floral-corner-image {
			width: 200px;
			height: 200px;
			object-fit: contain;
			opacity: 0.7;
			filter: brightness(1.1) contrast(1.1);
			transition: all 0.3s ease;
		}

		/* Decoraciones del medio de los bordes */
		.middle-decoration {
			position: absolute;
		}

		.floral-middle-image {
			width: 150px;
			height: 150px;
			object-fit: contain;
			opacity: 0.6;
			filter: brightness(0.8) contrast(1.2) sepia(0.3) hue-rotate(20deg) saturate(1.1);
			transition: all 0.3s ease;
		}

		/* Rotaciones en sentido horario */
		.rotated-90 {
			transform: rotate(90deg);
		}

		.rotated-180 {
			transform: rotate(180deg);
		}

		.rotated-270 {
			transform: rotate(270deg);
		}

		/* Posiciones de las esquinas */
		.corner-decoration.top-left {
			top: 0;
			left: 0;
		}

		.corner-decoration.top-right {
			top: 0;
			right: 0;
		}

		.corner-decoration.bottom-right {
			bottom: -10px;
			right: 0;
		}

		.corner-decoration.bottom-left {
			bottom: -10px;
			left: 0;
		}

		/* Posiciones del medio de cada borde - rotación antihoraria */
		.middle-decoration.center-top {
			top: -20px;
			left: 50%;
			transform: translateX(-50%) rotate(180deg);
		}

		.middle-decoration.center-right {
			right: -20px;
			top: 50%;
			transform: translateY(-50%) rotate(-90deg);
		}

		.middle-decoration.center-bottom {
			bottom: -30px;
			left: 50%;
			transform: translateX(-50%) rotate(0deg);
		}

		.middle-decoration.center-left {
			left: -20px;
			top: 50%;
			transform: translateY(-50%) rotate(-270deg);
		}

		/* Efectos hover */
		.hero:hover .floral-corner-image {
			opacity: 0.9;
			filter: brightness(1.2) contrast(1.2);
		}

		.hero:hover .floral-middle-image {
			opacity: 0.8;
			filter: brightness(0.9) contrast(1.3) sepia(0.4) hue-rotate(20deg) saturate(1.2);
		}

		/* Animación sutil */
		@keyframes floralGlow {
			0%, 100% { filter: brightness(1.1) contrast(1.1); }
			50% { filter: brightness(1.2) contrast(1.2); }
		}

		@keyframes floralGlowMiddle {
			0%, 100% { filter: brightness(0.8) contrast(1.2) sepia(0.3) hue-rotate(20deg) saturate(1.1); }
			50% { filter: brightness(0.9) contrast(1.3) sepia(0.4) hue-rotate(20deg) saturate(1.2); }
		}

		.floral-corner-image {
			animation: floralGlow 4s ease-in-out infinite;
		}

		/* Animación alternativa para las flores del medio */
		.floral-middle-image {
			animation: floralGlowMiddle 5s ease-in-out infinite;
			animation-delay: 1s;
		}			.hero-overlay {
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: rgba(0,0,0,0.3);
			}

			.hero-content {
				position: relative;
				z-index: 1;
			}

			.decorative-frame {
				position: relative;
				padding: 3rem 4rem;
				border: 3px solid;
				border-image: linear-gradient(45deg, #DAA520, #B8860B, #CD853F, #DAA520) 1;
				border-radius: 20px;
				margin: 2rem auto;
				max-width: 600px;
				background: rgba(255, 255, 255, 0.05);
			}

			.decorative-frame::before {
				content: '✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦';
				position: absolute;
				top: -8px;
				left: 50%;
				background: linear-gradient(45deg, #DAA520, #B8860B);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				font-size: 1.2rem;
				white-space: nowrap;
				text-shadow: 0 0 10px rgba(218, 165, 32, 0.5);
				animation: decorativeGlow 3s ease-in-out infinite, 
				          decorativeShimmer 5s linear infinite,
				          decorativeFloat 4s ease-in-out infinite;
			}

			.decorative-frame::after {
				content: '✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦ ❋ ✦';
				position: absolute;
				bottom: -8px;
				left: 50%;
				background: linear-gradient(45deg, #DAA520, #B8860B);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				font-size: 1.2rem;
				white-space: nowrap;
				text-shadow: 0 0 10px rgba(218, 165, 32, 0.5);
				animation: decorativeGlow 3s ease-in-out infinite 0.5s, 
				          decorativeShimmer 5s linear infinite 1s,
				          decorativeFloat 4s ease-in-out infinite 1s;
			}

			/* Decoraciones adicionales en las esquinas del marco */
			.decorative-frame .corner-ornament {
				position: absolute;
				width: 30px;
				height: 30px;
				background: linear-gradient(45deg, #DAA520, #B8860B);
				border-radius: 50%;
				opacity: 0.8;
				box-shadow: 0 0 15px rgba(218, 165, 32, 0.6);
			}

			.decorative-frame .corner-ornament::before {
				content: '❋';
				position: absolute;
				top: 50%;
				left: 50%;
				transform: translate(-50%, -50%);
				color: white;
				font-size: 1.2rem;
				text-shadow: 0 0 5px rgba(0, 0, 0, 0.3);
			}

			.decorative-frame .corner-ornament.top-left {
				top: -15px;
				left: -15px;
			}

			.decorative-frame .corner-ornament.top-right {
				top: -15px;
				right: -15px;
			}

			.decorative-frame .corner-ornament.bottom-left {
				bottom: -15px;
				left: -15px;
			}

			.decorative-frame .corner-ornament.bottom-right {
				bottom: -15px;
				right: -15px;
			}

			/* Decoraciones laterales */
			.decorative-frame .side-decoration {
				position: absolute;
				background: linear-gradient(45deg, #DAA520, #B8860B);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				font-size: 1.2rem;
				text-shadow: 0 0 10px rgba(218, 165, 32, 0.5);
				transform-origin: center;
			}

			.decorative-frame .side-decoration.left-side {
				left: -80px !important;
				top: 50%;
				transform: translateY(-50%) rotate(-90deg);
			}

			.decorative-frame .side-decoration.right-side {
				right: -80px !important;
				top: 50%;
				transform: translateY(-50%) rotate(90deg);
			}

			/* Animación para el marco decorativo */
			@keyframes frameGlow {
				0%, 100% { 
					box-shadow: 0 0 20px rgba(218, 165, 32, 0.3), 
					           inset 0 0 20px rgba(255, 255, 255, 0.1);
					transform: scale(1);
				}
				50% { 
					box-shadow: 0 0 40px rgba(218, 165, 32, 0.6), 
					           inset 0 0 30px rgba(255, 255, 255, 0.2);
					transform: scale(1.02);
				}
			}

			@keyframes borderShimmer {
				0% { 
					border-image: linear-gradient(45deg, #DAA520, #B8860B, #CD853F, #DAA520) 1;
				}
				25% { 
					border-image: linear-gradient(90deg, #B8860B, #CD853F, #DAA520, #B8860B) 1;
				}
				50% { 
					border-image: linear-gradient(135deg, #CD853F, #DAA520, #B8860B, #CD853F) 1;
				}
				75% { 
					border-image: linear-gradient(180deg, #DAA520, #B8860B, #CD853F, #DAA520) 1;
				}
				100% { 
					border-image: linear-gradient(45deg, #DAA520, #B8860B, #CD853F, #DAA520) 1;
				}
			}

			@keyframes frameFloat {
				0%, 100% { 
					transform: translateY(0px);
				}
				50% { 
					transform: translateY(-5px);
				}
			}

			@keyframes ornamentPulse {
				0%, 100% { 
					transform: scale(1) rotate(0deg);
					box-shadow: 0 0 15px rgba(218, 165, 32, 0.6);
				}
				50% { 
					transform: scale(1.2) rotate(180deg);
					box-shadow: 0 0 25px rgba(218, 165, 32, 0.9),
					           0 0 35px rgba(218, 165, 32, 0.4);
				}
			}

			@keyframes ornamentGlow {
				0%, 100% {
					background: linear-gradient(45deg, #DAA520, #B8860B);
				}
				50% {
					background: linear-gradient(135deg, #CD853F, #DAA520);
				}
			}

			.decorative-frame {
				animation: frameGlow 4s ease-in-out infinite, 
				          borderShimmer 6s linear infinite, 
				          frameFloat 3s ease-in-out infinite;
			}

			.decorative-frame .corner-ornament {
				animation: ornamentPulse 6s ease-in-out infinite, 
				          ornamentGlow 4s ease-in-out infinite;
			}

			.decorative-frame .corner-ornament:nth-child(2) {
				animation-delay: 0.5s;
			}

			.decorative-frame .corner-ornament:nth-child(3) {
				animation-delay: 1s;
			}

			.decorative-frame .corner-ornament:nth-child(4) {
				animation-delay: 1.5s;
			}

			/* Animación para las decoraciones laterales */
			.decorative-frame .side-decoration {
				animation: decorativeGlow 4s ease-in-out infinite, 
				          decorativeShimmer 6s linear infinite;
			}

			.decorative-frame .side-decoration.left-side {
				animation: decorativeGlow 4s ease-in-out infinite, 
				          decorativeShimmer 6s linear infinite,
				          sideDecorativeFloat 5s ease-in-out infinite;
				animation-delay: 1s, 2s, 2.5s;
			}

			.decorative-frame .side-decoration.right-side {
				animation: decorativeGlow 4s ease-in-out infinite, 
				          decorativeShimmer 6s linear infinite,
				          sideDecorativeFloatRight 5s ease-in-out infinite;
				animation-delay: 1.5s, 3s, 3.5s;
			}

			@keyframes decorativeGlow {
				0%, 100% { 
					text-shadow: 0 0 10px rgba(218, 165, 32, 0.5);
					transform: scale(1);
				}
				50% { 
					text-shadow: 0 0 20px rgba(218, 165, 32, 0.9), 
					           0 0 30px rgba(218, 165, 32, 0.6);
					transform: scale(1.1);
				}
			}

			@keyframes decorativeShimmer {
				0%, 100% {
					background: linear-gradient(45deg, #DAA520, #B8860B);
					-webkit-background-clip: text;
					-webkit-text-fill-color: transparent;
				}
				25% {
					background: linear-gradient(90deg, #B8860B, #CD853F);
					-webkit-background-clip: text;
					-webkit-text-fill-color: transparent;
				}
				50% {
					background: linear-gradient(135deg, #CD853F, #DAA520);
					-webkit-background-clip: text;
					-webkit-text-fill-color: transparent;
				}
				75% {
					background: linear-gradient(180deg, #DAA520, #B8860B);
					-webkit-background-clip: text;
					-webkit-text-fill-color: transparent;
				}
			}

			@keyframes decorativeFloat {
				0%, 100% { 
					transform: translateX(-50%) translateY(0px);
				}
				25% { 
					transform: translateX(-50%) translateY(-1px);
				}
				50% { 
					transform: translateX(-50%) translateY(0px);
				}
				75% { 
					transform: translateX(-50%) translateY(1px);
				}
			}

			@keyframes sideDecorativeFloat {
				0%, 100% { 
					transform: translateY(-50%) rotate(-90deg) scale(1);
				}
				50% { 
					transform: translateY(-50%) rotate(-90deg) scale(1.05);
				}
			}

			@keyframes sideDecorativeFloatRight {
				0%, 100% { 
					transform: translateY(-50%) rotate(90deg) scale(1);
				}
				50% { 
					transform: translateY(-50%) rotate(90deg) scale(1.05);
				}
			}

			.hero-title {
				font-family: 'Dancing Script', cursive;
				font-size: 5.5rem;
				font-weight: 700;
				margin-bottom: 1rem;
				text-shadow: 3px 3px 6px rgba(0,0,0,0.6);
			}

			.hero-subtitle {
				font-size: 2rem;
				margin-bottom: 2rem;
				font-weight: 400;
				text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
			}

			.hero-decoration {
				font-size: 2rem;
				animation: sparkle 2s ease-in-out infinite;
			}

			@keyframes sparkle {
				0%, 100% { transform: scale(1); }
				50% { transform: scale(1.1); }
			}

			/* Section Styles */
			section {
				padding: 80px 0;
			}

			.section-title {
				font-family: 'Dancing Script', cursive;
				font-size: 3rem;
				text-align: center;
				margin-bottom: 3rem;
				color: #8B7355;
			}

			/* Countdown Section */
			.countdown-section {
				background: linear-gradient(45deg, #DAA520 0%, #CD853F 100%);
				color: white;
			}

			.countdown-section .section-title {
				color: white;
			}

			.countdown {
				display: flex;
				justify-content: center;
				gap: 2rem;
				flex-wrap: wrap;
			}

			.countdown-item {
				background: rgba(255,255,255,0.2);
				padding: 2rem;
				border-radius: 15px;
				text-align: center;
				backdrop-filter: blur(10px);
				border: 1px solid rgba(255,255,255,0.3);
			}

			.countdown-number {
				display: block;
				font-size: 3rem;
				font-weight: 600;
				margin-bottom: 0.5rem;
			}

			.countdown-label {
				font-size: 1rem;
				text-transform: uppercase;
				letter-spacing: 1px;
			}

			/* Event Details */
			.event-details {
				background: #f8f9fa;
			}

			.event-info {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
				gap: 2rem;
				margin-bottom: 3rem;
			}

			.event-item {
				display: flex;
				align-items: center;
				gap: 1rem;
				background: white;
				padding: 2rem;
				border-radius: 10px;
				box-shadow: 0 5px 15px rgba(0,0,0,0.1);
			}

			.event-item i {
				font-size: 2rem;
				color: #8B7355;
			}

			.action-buttons {
				display: flex;
				gap: 1rem;
				justify-content: center;
				flex-wrap: wrap;
			}

			.btn {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				padding: 1rem 2rem;
				border: none;
				border-radius: 50px;
				text-decoration: none;
				font-weight: 500;
				transition: all 0.3s ease;
				cursor: pointer;
				font-size: 1rem;
			}

			.btn-primary {
				background: linear-gradient(45deg, #B8860B, #8B7355);
				color: white;
			}

			.btn-secondary {
				background: white;
				color: #8B7355;
				border: 2px solid #8B7355;
			}

			.btn:hover {
				transform: translateY(-2px);
				box-shadow: 0 10px 25px rgba(0,0,0,0.2);
			}

			/* Dress Code */
			.dress-code {
				background: white;
			}

			.dress-code-content {
				text-align: center;
			}

			.dress-code-item {
				margin-bottom: 2rem;
			}

			.dress-code-item i {
				font-size: 3rem;
				color: #8B7355;
				margin-bottom: 1rem;
			}

			.color-palette {
				margin-top: 2rem;
			}

			.colors {
				display: flex;
				justify-content: center;
				gap: 1rem;
				margin-top: 1rem;
				flex-wrap: wrap;
			}

			.color-item {
				padding: 1rem 1.5rem;
				border-radius: 25px;
				color: #333;
				font-weight: 500;
				box-shadow: 0 3px 10px rgba(0,0,0,0.2);
			}

			/* Location */
			.location {
				background: #f8f9fa;
			}

			.location-content {
				display: grid;
				grid-template-columns: 1fr 1fr;
				gap: 3rem;
				align-items: center;
			}

			.location-info h3 {
				color: #8B7355;
				margin-bottom: 1rem;
			}

			.location-info p {
				margin-bottom: 0.5rem;
			}

			.map-container {
				border-radius: 10px;
				overflow: hidden;
				box-shadow: 0 5px 15px rgba(0,0,0,0.1);
			}

			/* Music Suggestions */
			.music-suggestions {
				background: linear-gradient(45deg, #B8860B, #8B7355);
				color: white;
				text-align: center;
			}

			.music-suggestions .section-title {
				color: white;
			}

			.music-content p {
				font-size: 1.1rem;
				margin-bottom: 1rem;
			}

			/* Gifts */
			.gifts {
				background: white;
				text-align: center;
			}

			.gifts-content p {
				font-size: 1.1rem;
				margin-bottom: 2rem;
				color: #666;
			}

			/* Footer */
			.footer {
				background: #333;
				color: white;
				text-align: center;
				padding: 2rem 0;
			}

			.footer-contact {
				margin-top: 3rem;
				padding-top: 2rem;
				border-top: 2px solid rgba(255,255,255,0.1);
			}

			.footer-contact p {
				font-size: 1.1rem;
				margin-bottom: 0;
				color: #fff;
			}

			.whatsapp-link {
				display: inline-flex;
				align-items: center;
				gap: 0.3rem;
				background: #25D366;
				color: white;
				padding: 0.4rem 1rem;
				border-radius: 20px;
				text-decoration: none;
				font-weight: 500;
				transition: all 0.3s ease;
				margin-left: 0.5rem;
				font-size: 0.95rem;
			}

			.whatsapp-link:hover {
				background: #1DA851;
				transform: translateY(-1px);
				box-shadow: 0 3px 10px rgba(37, 211, 102, 0.3);
			}

			.whatsapp-link i {
				font-size: 1.1rem;
			}

			.footer-credits {
				margin-top: 2rem;
				padding-top: 1rem;
				border-top: 1px solid rgba(255,255,255,0.1);
			}

			.footer-credits p {
				font-size: 0.9rem;
				opacity: 0.8;
				color: #ccc;
			}

			.footer-credits a {
				color: #B8860B;
				text-decoration: none;
				transition: all 0.3s ease;
			}

			.footer-credits a:hover {
				color: #8B7355;
				text-decoration: underline;
			}

			.footer-credits strong {
				font-weight: 600;
			}

			/* Responsive */
			@media (max-width: 768px) {
				.hero-title {
					font-size: 4.5rem;
				}

				.hero-subtitle {
					font-size: 1.7rem;
				}

				.section-title {
					font-size: 2.5rem;
				}

				.countdown {
					gap: 1rem;
				}

				.countdown-item {
					padding: 1.5rem;
				}

				.countdown-number {
					font-size: 2rem;
				}

				.location-content {
					grid-template-columns: 1fr;
				}

				.action-buttons {
					flex-direction: column;
					align-items: center;
				}

				/* Flores más grandes para móviles - tamaño mejorado */
				.floral-corner-image {
					width: 160px;
					height: 160px;
				}

				.floral-middle-image {
					width: 120px;
					height: 120px;
				}

				/* Marco decorativo responsive */
				.decorative-frame {
					padding: 2rem 2.5rem;
					margin: 1rem auto;
					border-radius: 15px;
				}

				.decorative-frame::after {
					font-size: 1rem;
					top: -6px;
				}

				.decorative-frame::before {
					font-size: 1rem;
					bottom: -6px;
				}

				.decorative-frame .corner-ornament {
					width: 25px;
					height: 25px;
				}

				.decorative-frame .corner-ornament::before {
					font-size: 1rem;
				}

				.decorative-frame .side-decoration {
					font-size: 1rem;
				}

				.decorative-frame .side-decoration.left-side {
					left: -65px !important;
				}

				.decorative-frame .side-decoration.right-side {
					right: -65px !important;
				}
			}

			@media (max-width: 480px) {
				.hero-title {
					font-size: 3.5rem;
				}

				.hero-subtitle {
					font-size: 1.4rem;
				}

				.floral-corner-image {
					width: 140px;
					height: 140px;
					opacity: 0.6;
				}

				.floral-middle-image {
					width: 100px;
					height: 100px;
					opacity: 0.5;
				}

				/* Marco decorativo en móviles pequeños */
				.decorative-frame {
					padding: 1.5rem 2rem;
					margin: 0.5rem auto;
					border-radius: 12px;
				}

				.decorative-frame::before {
					border-radius: 20px;
				}

				.decorative-frame::after {
					font-size: 0.9rem;
					top: -5px;
				}

				.decorative-frame::before {
					font-size: 0.9rem;
					bottom: -5px;
				}

				.decorative-frame .corner-ornament {
					width: 20px;
					height: 20px;
				}

				.decorative-frame .corner-ornament::before {
					font-size: 0.9rem;
				}

				.decorative-frame .side-decoration {
					font-size: 0.9rem;
				}

				.decorative-frame .side-decoration.left-side {
					left: -50px !important;
				}

				.decorative-frame .side-decoration.right-side {
					right: -50px !important;
				}
			}
		</style>

		<script>
			// Add to Calendar function - disponible globalmente
			(window as any).addToCalendar = function() {
				console.log('Función addToCalendar ejecutada');
				
				const eventDetails = {
					title: 'Cumpleaños de 15 años - Milena',
					   start: '20251018T213000',
					   end: '20251019T053000',
					description: '¡Fiesta de los 15 años de Milena!',
					location: 'Janos Champagnat, San Martín 71, B1629ETA Pilar, Provincia de Buenos Aires'
				};

				const googleCalendarUrl = `https://calendar.google.com/calendar/render?action=TEMPLATE&text=${encodeURIComponent(eventDetails.title)}&dates=${eventDetails.start}/${eventDetails.end}&details=${encodeURIComponent(eventDetails.description)}&location=${encodeURIComponent(eventDetails.location)}`;
				
				console.log('URL generada:', googleCalendarUrl);
				
				try {
					window.open(googleCalendarUrl, '_blank');
				} catch (error) {
					console.error('Error al abrir Google Calendar:', error);
					// Fallback: copiar URL al portapapeles
					navigator.clipboard.writeText(googleCalendarUrl).then(() => {
						alert('No se pudo abrir Google Calendar automáticamente. La URL se ha copiado al portapapeles. Pégala en tu navegador para agregar el evento.');
					}).catch(() => {
						alert('Por favor, abre este enlace en tu navegador: ' + googleCalendarUrl);
					});
				}
			};

			// Countdown Timer
			function updateCountdown() {
				   const eventDate = new Date('2025-10-18T21:30:00').getTime();
				const now = new Date().getTime();
				const distance = eventDate - now;

				if (distance > 0) {
					const days = Math.floor(distance / (1000 * 60 * 60 * 24));
					const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
					const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
					const seconds = Math.floor((distance % (1000 * 60)) / 1000);

					const daysEl = document.getElementById('days');
					const hoursEl = document.getElementById('hours');
					const minutesEl = document.getElementById('minutes');
					const secondsEl = document.getElementById('seconds');

					if (daysEl) daysEl.textContent = days.toString().padStart(2, '0');
					if (hoursEl) hoursEl.textContent = hours.toString().padStart(2, '0');
					if (minutesEl) minutesEl.textContent = minutes.toString().padStart(2, '0');
					if (secondsEl) secondsEl.textContent = seconds.toString().padStart(2, '0');
				} else {
					const countdownEl = document.getElementById('countdown');
					if (countdownEl) countdownEl.innerHTML = '<h2>¡La fiesta empezó! 🎉</h2>';
				}
			}

			// Update countdown every second
			setInterval(updateCountdown, 1000);
			updateCountdown();
		</script>
	</body>
</html>
